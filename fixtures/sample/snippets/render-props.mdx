<CodeSurfer
  title={() => (
    <h1 style={{ background: "pink" }}>
      Use Render Props
    </h1>
  )}
  notes={() => (
    <span style={{ background: "pink" }}>
      For title or notes
    </span>
  )}
  code={require("!raw-loader!./snippets/3.mdx")}
/>
